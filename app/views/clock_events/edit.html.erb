<h1>Editing Clock Event</h1>

<%= render 'form', clock_event: @clock_event %>

<%= link_to 'Show', teacher_clock_event_path(@teacher, @clock_event) %> |
<%= link_to 'Back', teacher_clock_events_path(@teacher) %>
